<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">
    <environmentMode>REPRODUCIBLE</environmentMode>
    <!-- Define the model -->
    <solutionClass>lv.lu.df.combopt.domain.NavigationSolution</solutionClass>
    <entityClass>lv.lu.df.combopt.domain.TaskLocation</entityClass>

    <!-- Define the score function -->
    <scoreDirectorFactory>
        <constraintProviderClass>lv.lu.df.combopt.solver.StreamCalculator</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Configure the optimization algorithms (optional) -->
    <termination>
        <secondsSpentLimit>10</secondsSpentLimit>
    </termination>

    <constructionHeuristic>

    </constructionHeuristic>

    <localSearch>
        <unionMoveSelector>
            <subChainChangeMoveSelector>
                <entityClass>
                    lv.lu.df.combopt.domain.TaskLocation
                </entityClass>
                <subChainSelector>
                    <valueSelector variableName="prev"/>
                </subChainSelector>
                <valueSelector variableName="prev"/>
            </subChainChangeMoveSelector>
            <changeMoveSelector>
                <entitySelector id="taskSelector">
                    <entityClass>lv.lu.df.combopt.domain.TaskLocation</entityClass>
                </entitySelector>
                <valueSelector variableName="isVisited"/>
            </changeMoveSelector>
        </unionMoveSelector>
        <acceptor>
            <valueTabuSize>2</valueTabuSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>1000</acceptedCountLimit>
        </forager>
    </localSearch>
</solver>